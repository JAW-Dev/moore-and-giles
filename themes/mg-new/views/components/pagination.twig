<div class="pagination">
    {% if posts.pagination.next %}
        <a href="{{posts.pagination.next.link}}" class="button next {{posts.pagination.next.link|length ? '' : 'invisible'}}">View Older Posts</a>
    {% endif %}
    {% if posts.pagination.prev %}
        <a href="{{posts.pagination.prev.link}}" class="button prev {{posts.pagination.prev.link|length ? '' : 'invisible'}}">View Newer Posts</a>
    {% endif %}
    {# <ul class="pages">
        {% for page in posts.pagination.pages %}
            <li>
                {% if page.link %}
                    <a href="{{page.link}}" class="{{page.class}}">{{page.title}}</a>
                {% else %}
                    <span class="{{page.class}}">{{page.title}}</span>
                {% endif %}
            </li>
        {% endfor %}
    </ul> #}
</div>
